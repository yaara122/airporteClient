to fix list:

server:

react:

unable navigatin to unothorized user
add 404 page


done:
//add validation to req body statuse is valid value - request setStatus
//get id of the request in the params and not the body request setStatus
//request provier set data if exsists in the response
//add validation to make sure usernamne is not taken + enable save while doing so ( user model)
//make a general get request function in reqProvider that will return requestItems List
//make error of password is incorrect and in general more clear userlogin
//and errors in general


//סוגי הערות מהשרת

להשתמש בהודעות מהשרת או לעשות שגיאות כלליות?

לא מצליח להירשם
לא מצליח להתחבר

לא מצליח לייבא בקשות
לא מצליח ליצר בקשה
לא מצליח לעדכן בקשה

